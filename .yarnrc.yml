nodeLinker: node-modules            # create node_modules (most React libs expect this)
enableGlobalCache: true
checksumBehavior: update            # avoid strict failures if checksums change (you can switch to "throw" later)
compressionLevel: 0                 # faster CI
yarnPath: null                      # let Corepack manage Yarn (no .yarn/releases needed)
