# .yarnrc.yml (minimal, Yarn 4)
nodeLinker: node-modules
enableGlobalCache: true
checksumBehavior: update
# IMPORTANT: do NOT include "networkTimeout" (legacy) or "yarnPath"
# If you need a custom registry, this is OK:
# npmRegistryServer: "https://registry.npmjs.org"
